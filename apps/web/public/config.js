// Replaced at deploy time. Values left empty fall back to the build-time Vite
// variables, which is what the development server relies on.
window.__SCHEMATIC_CONFIG__ = {
  apiUrl: '',
  collabUrl: '',
};
