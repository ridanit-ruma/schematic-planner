# Workspace packages resolve to the local checkout, never the registry.
link-workspace-packages=true
prefer-workspace-packages=true
# Surface accidental reliance on transitive dependencies at install time.
strict-peer-dependencies=false
auto-install-peers=true
